/** Minimum server-side Support tally (story + saved reactions) to post a discussion link. */
export const MIN_SUPPORT_TO_HOST_MEETING = 1000;

/** Limit spam: max hosted meetings recorded per story. */
export const MAX_MEETINGS_PER_STORY = 5;
