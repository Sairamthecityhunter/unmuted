# Unmuted

The Next.js application for this project lives in **`web/`**.

Documentation (setup, folder structure, **world-visible community posts**, Vercel + Redis): **[web/README.md](web/README.md)**.

HTML files in this folder are static prototypes; the production UI is implemented under `web/src/`.
